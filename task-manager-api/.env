# DB
DB__ENDPOINT=postgresql://neondb_owner:npg_w7nM6gteFvBq@ep-jolly-pond-a1hhbuo8-pooler.ap-southeast-1.aws.neon.tech/taskflow-db?sslmode=require&channel_binding=require
DB__MIN_SIZE=5
DB__MAX_SIZE=25
DB__TIMEOUT=60
DB__MAX_IDLE=600


# JWT
JWT__ACCESS_TOKEN_SECRET=secret 
JWT__REFRESH_TOKEN_SECRET=secret

# Maps to settings.jwt.refresh_token_secret
# The secret must be present as it has no default value.

# --- Optional Ages (Overriding Defaults) ---

# Maps to settings.jwt.access_token_age
# Override the default 3600 (1 hour) to 900 (15 minutes)
JWT__ACCESS_TOKEN_AGE=900

# Maps to settings.jwt.refresh_token_age
# Override the default 604800 (1 week) to 2592000 (30 days)
JWT__REFRESH_TOKEN_AGE=2592000
OAUTH2__CLIENT_ID="YOUR_REAL_GOOGLE_CLIENT_ID"

# Overrides client_secret="mock"
OAUTH2__CLIENT_SECRET="YOUR_REAL_GOOGLE_CLIENT_SECRET"

# Overrides redirect_uri="http://localhost:8000/auth/callback"
# Note: Ensure this matches the URI registered in the Google Developer Console
OAUTH2__REDIRECT_URI="https://api.yourdomain.com/auth/google/callback"

# --- Logger Configuration Overrides ---

# Overrides level="INFO" to increase verbosity
# Maps to settings.logger.level
LOGGER__LEVEL="DEBUG"

# Overrides file="app.log" to direct logs to a different location
# Maps to settings.logger.file
LOGGER__FILE="./app.log"

# Overrides console=True to disable console output
# Pydantic is case-insensitive for booleans, but 'False' or '0' is standard
# Maps to settings.logger.console
LOGGER__CONSOLE=True

# Overrides the default log format (less common, but possible)
# Maps to settings.logger.fmt
LOGGER__FMT="%(levelname)s: [%(filename)s:%(lineno)d] %(message)s"
GOOGLE_APPLICATION_CREDENTIALS=/home/baothai/.config/gcloud/application_default_credentials.json
GCS__BUCKET=dev-taskflow-bucket
